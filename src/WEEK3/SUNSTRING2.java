package WEEK3;

public class SUNSTRING2 {
 public static void main(String[] args) {
	 String str = "Programming";
	 // start is included but end is not included
	 str = str.substring(0,str.length()-1);
	 str = str.substring(0,str.length()-1);
	 str = str.substring(0,str.length()-1);
	 str = str.substring(0,str.length()-1);
	 System.out.println(str);
	 
 }
}
